import com.manning.gia.ProjectVersion

ext.buildTimestamp = new Date().format('yyyy-MM-dd HH:mm:ss')
project.version = new ProjectVersion(version_major, version_minor, version_fix, System.env.SOURCE_BUILD_NUMBER)
